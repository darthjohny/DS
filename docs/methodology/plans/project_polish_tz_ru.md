# План вылизывания и причесывания проекта

Дата фиксации: `2026-04-06`

Связанные документы:

- [project_cleanup_tz_ru.md](/Users/evgeniikuznetsov/Desktop/dspro-vkr/docs/methodology/plans/project_cleanup_tz_ru.md)
- [regression_testing_tz_ru.md](/Users/evgeniikuznetsov/Desktop/dspro-vkr/docs/methodology/plans/regression_testing_tz_ru.md)
- [vkr_interpretation_tz_ru.md](/Users/evgeniikuznetsov/Desktop/dspro-vkr/docs/methodology/plans/vkr_interpretation_tz_ru.md)

## Зачем нужен этот план

Основной контур проекта уже собран:

- контур стабилен;
- регрессионный слой введен;
- документы для ВКР и публичный `README` приведены в порядок;
- рабочий прогон воспроизводится.

Теперь нужен не новый виток глубокой проверки и не новая научная волна, а
спокойное вылизывание проекта:

- найти лишние фасады;
- проверить, не осталось ли мертвых файлов и случайно заехавших хвостов;
- дочистить рабочее дерево и тестовый слой;
- убедиться, что публичный контур выглядит аккуратно и без мелкого шума.

## Цель

Провести легкую, но системную уборку активного контура проекта без изменения
основной логики и без новой волны переделок.

Этот пакет должен ответить на вопрос:

«Не осталось ли в проекте мелкого технического мусора, лишних оберток, мертвых
файлов и случайных хвостов, которые не ломают систему, но портят зрелость
репозитория?»

## Что проверяем

### Фасады и публичные границы

Нужно перепроверить:

- `__init__.py` в активных пакетах;
- тонкие входные файлы CLI;
- публичные вспомогательные модули в `reporting`, `db`, `posthoc`, `datasets`;
- совместимые пути и старые адаптеры.

### Мертвый код и случайные файлы

Нужно поискать:

- неиспользуемые модули;
- вспомогательные файлы без реальных импортов;
- локальные дубликаты и случайные файлы;
- старые fixtures и тесты, которые дублируют новый слой.

### Рабочее дерево и локальный мусор

Нужно дочистить:

- `__pycache__` в активном контуре;
- корневой мелкий мусор;
- локальные каталоги, которые должны быть либо игнорируемыми, либо явно
  осмысленными.

### Тестовый контур после введения `regression`

Нужно проверить:

- нет ли пересечения и дублирования между `unit`, `regression`, `smoke`,
  `integration`;
- не остались ли старые helpers или fixtures, которые можно убрать;
- нет ли тестов, которые формально живы, но уже не добавляют новой страховки.

### Публичная документация и навигация

Нужно перепроверить:

- не осталось ли устаревших ссылок после разведения публичного и внутреннего
  слоев;
- не висит ли лишняя навигация по старым пакетам;
- не появились ли документы, которые уже не нужны на публичной витрине.

## Что не входит в этот план

В этот пакет не входят:

- новая функциональность;
- новые модели и новое обучение;
- повторная большая волна проверки по всему проекту;
- удаление архивного слоя `archive_research`;
- новая презентация или новая научная интерпретация.

## Ожидаемый результат

Пакет считается закрытым, когда:

- из активного контура убраны лишние фасады и явные мертвые хвосты;
- тестовое дерево остается зеленым и при этом становится чище;
- рабочее дерево не содержит мелкого технического мусора в активных каталогах;
- публичная документация не содержит случайных внутренних или устаревших
  ссылок;
- проект остается таким же воспроизводимым, но визуально и структурно
  аккуратнее.
